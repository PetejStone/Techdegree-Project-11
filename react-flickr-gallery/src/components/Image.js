import React from 'react';
import App from '../App.js';

const Image = (props) => (
    
        <li className="image">
            <img src={props.url} alt="" />
    
        </li>
    
);

export default Image;